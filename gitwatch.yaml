---# Github watched repos/branches
  substack:
    stackvm:
      master:
        channels:
          - "#stackvm"
    dnode:
      master:
        channels:
          - "#stackvm"
    js-traverse:
      master:
        channels:
          - "#stackvm"
  pkrumins:
    stackvm:
      master:
        channels:
          - "#stackvm"
    node-jsmin:
      master:
        channels:
          - "#stackvm"
    node-jpeg:
      master:
        channels:
          - "#stackvm"
    node-gif:
      master:
        channels:
          - "#stackvm"
    node-png:
      master:
        channels:
          - "#stackvm"
    node-video:
      master:
        channels:
          - "#stackvm"
  jesusabdullah:
    lulzbot:
      master:
        channels:
          - "#stackvm"
      trackbranchES:
        channels:
          - "#stackvm"
    "jesusabdullah.github.com":
      master:
        channels:
          - "#stackvm"
