---# Github watched repos/branches
  substack:
    stackvm:
      master:
        channels:
          - "#stackvm"
    dnode:
      master:
        channels:
          - "#stackvm"
    js-traverse:
      master:
        channels:
          - "#stackvm"
  pkrumins:
    stackvm:
      master:
        channels:
          - "#stackvm"
    node-jsmin:
      master:
        channels:
          - "#stackvm"
  jesusabdullah:
    lulzbot:
      master:
        channels:
          - "#stackvm"
    "jesusabdullah.github.com":
      master:
        channels:
          - "#stackvm"
